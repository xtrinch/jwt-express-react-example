const INITIAL_STATE = {
	users: "Oh my god there's just so many users here"
}

const usersReducer = (currentState = INITIAL_STATE, action) => {
  switch (action.type) {
	default:
	       return currentState;
	}
}

export default usersReducer;